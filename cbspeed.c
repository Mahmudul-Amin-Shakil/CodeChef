#include <stdio.h>
int main()
{   
        int x, y;
        scanf("%d%d", &x, &y);
        if (x >= y)
        {
            printf("no\n");
        }
        else
        {
            printf("yes\n");
        }
    return 0;
}