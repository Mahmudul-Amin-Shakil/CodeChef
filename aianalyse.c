#include <stdio.h>
int main()
{
        int x;
        scanf("%d", &x);
        if (1000 >= x)
        {
            printf("yes\n");
        }
        else
        {
            printf("no\n");
        }
    return 0;
}